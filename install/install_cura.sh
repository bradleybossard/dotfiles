#!/bin/bash
# Ignore the PPA for now, best way seems to be downloading from site
# In the future, check for a snap package
# sudo add-apt-repository ppa:thopiekar/cura-master
# sudo apt-get update
# sudo apt-get install --yes cura
