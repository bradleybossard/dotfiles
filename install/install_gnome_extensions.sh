#!/bin/bash
# Installs the Gnome Extensions app
sudo apt install --yes gnome-shell-extensions
# Allows installation of extensions via https://extensions.gnome.org/
sudo apt-get install --yes chrome-gnome-shell

# https://extensions.gnome.org/extension/307/dash-to-dock/
